## What changed

## Why

## How it was tested

- [ ] `macos`: `./build.sh` and `--selftest` pass
- [ ] `android`: `./gradlew :shared:jvmTest :androidApp:assembleDebug` pass
- [ ] Tested on hardware (Mac model, phone model, USB or Wi-Fi):
- [ ] Protocol changes are reflected in `docs/PROTOCOL.md`, both implementations and `tools/probe.py`
